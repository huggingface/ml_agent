# Ralph progress log — bg-sessions-mongo-control-plane

## Session start: 2026-05-01

### Pipeline upstream
- /deep-dive: 8 interview rounds + 3-lane causal trace (Mongo confirmed configured in prod, sticky-session question sidestepped, heartbeat reframed via lease+heartbeat)
- /plan --consensus --direct: 2 iterations. Architect APPROVE WITH SUGGESTIONS → Critic APPROVE WITH REQUIRED REVISIONS → Planner revision → Architect APPROVE → Critic APPROVE WITH MINOR CLEANUP → cleanups applied directly.
- Final plan: .omc/plans/ralplan-enable-ml-intern-to-run.md (453 lines, 7 steps + Step 1.5)
- Branch: bg-sessions-mongo-control-plane (commit 7da6dc9: design docs)

### PRD: 8 stories (US-001 through US-008)
- US-001: Mongo persistence layer extensions (Step 1) — IN PROGRESS
- US-002: requeue_claimed_for wiring (Step 1.5)
- US-003: SessionManager lease awareness + heartbeat (Step 2)
- US-004: Replace submission_queue + add is_in_tool_call (Step 3)
- US-005: Holder-local fast-path overlay for SSE (Step 4)
- US-006: Lifespan sweep + grace sweeper + /background + migrating event + subscriber counts (Step 5)
- US-007: Worker entrypoint + MODE flag + idle eviction (Step 6)
- US-008: Observability + deployment migration (Step 7)

### Locked open questions (from plan)
1. Backward-compat: option (a) backfill empty lease for sessions with last_active_at > now-1h; flip older to runtime_state=idle (NOT ended, recoverable)
2. holder_id: f"{mode}:{hostname}:{uuid7-or-uuid4-suffix}" — uuid7 if Python ≥ 3.13 else uuid4
3. Idle eviction: 30 min default, predicate `not is_in_tool_call AND not is_processing AND no pending_submissions`

### Codebase notes from prior reads
- backend/session_manager.py: 1014 lines. EventBroadcaster at 44-78. AgentSession dataclass at 81-101. submission_queue field at 88, constructed at 535. _run_session at 693-772 (1s timeout polling on submission_queue.get).
- backend/main.py: 115 lines. lifespan at 30-63. Insertion point: between line 62 (close of try/except for final-flush) and line 63 (await session_manager.close()).
- backend/start.sh: 15 lines (verified by wc -l). Has port-conflict graceful exit-0 hack to preserve.
- agent/core/session_persistence.py: NoopSessionStore at 54-103, MongoSessionStore at 105-472, get_session_store at 478. Uses AsyncMongoClient, counters collection for monotonic seq.
- agent/core/agent_loop.py: tool dispatch at lines ~1111 and ~1387 (per critic). Plus agent/tools/research_tool.py:~460.

## Iteration 1: US-001 (Step 1 — Mongo persistence layer extensions) — COMPLETE

Delegated to executor at OPUS tier. Result:
- agent/core/session_persistence.py: +319 lines (10 new MongoSessionStore methods + 5 NoopSessionStore mirrors + make_holder_id helper + _backfill_lease_state + new pending_submissions index)
- pyproject.toml: +1 line (mongomock-motor>=0.0.36 to dev deps)
- tests/test_session_persistence_lease.py: NEW, 12 tests all passing (1.75s)
- ruff: clean
- Pre-existing tests still pass (11 in tests/unit/test_session_persistence.py + tests/unit/test_session_manager_persistence.py)

Verified anchors:
- make_holder_id() at line 35
- _backfill_lease_state() called from init() at line 177; defined at 187
- claim_lease/renew_lease/release_lease at lines 566/603/620 (MongoSessionStore)
- claim_pending_submission at 659; requeue_claimed_for at 698
- NoopSessionStore mirrors at 123-141

Time: ~3 min wall, ~63k tokens executor. Commit follows.

## Iteration 2: US-002 + US-003 (Steps 1.5 + 2 combined) — COMPLETE

Dispatched as one executor task (Opus) since both touch backend/session_manager.py.

Result:
- backend/session_manager.py: __init__ reads MODE env (validates main|worker, defaults main on invalid with WARN), computes self.mode + self._holder_id via make_holder_id, initializes self._heartbeat_task = None
- start() launches _lease_heartbeat_loop (10s cadence = TTL/3)
- close() cancels and awaits heartbeat task
- _lease_heartbeat_loop snapshots session keys under lock, renews each lease, routes None to _on_lease_lost
- _on_lease_lost calls requeue_claimed_for(holder_id), pops session, cancels task without awaiting, WARN log
- create_session: claim_lease before _start_agent_session; RuntimeError if None and store enabled
- ensure_session_loaded: claim_lease after ownership check; returns None on claim failure (refuses cross-process restore)
- _run_session finally: release_lease after _cleanup_sandbox
- shutdown_session + delete_session: release_lease before task.cancel
- tests/test_session_manager_lease.py NEW (12 tests): holder identity (default/worker/invalid), claim wiring, heartbeat renewal, Noop skip, lease-loss handling, release in finally, close cancellation
- tests/unit/test_session_manager_persistence.py: helper updated to seed new __init__ attributes

Verification: 35 tests pass (12 new + 12 from US-001 + 11 pre-existing); ruff clean.

## Iteration 3: US-004 (Step 3 — replace submission_queue + is_in_tool_call) — COMPLETE

Heart of the refactor. Result:

backend/session_manager.py:
- Removed submission_queue field from AgentSession; added last_submission_at: float for US-007.
- Removed local submission_queue construction in 3 places (_create_session_sync, ensure_session_loaded, create_session).
- Added _consume_submissions(agent_session): change-stream-with-poll-fallback consumer that drains via _drain_and_process on every signal.
- Added _drain_and_process(agent_session): atomic FIFO claim loop. Handles op_type='interrupt' inline (calls session.cancel()) and op_type='shutdown' inline (session.is_running=False); other ops dispatch through process_submission. Marks every claimed submission done in finally so poison rows never redeliver.
- Added _build_operation(op_type, payload).
- Replaced _run_session's submission while/get/timeout loop with await self._consume_submissions(agent_session). Signature now (self, session_id, event_queue, tool_router).
- Added shared helper _enqueue_or_false: gates on in-memory presence OR store.load_session() for cross-process delivery.
- Rewrote submit, submit_user_input, submit_approval, undo, compact, shutdown_session to enqueue via the helper.
- Rewrote interrupt: holder fast-path direct cancel; non-holder enqueues op_type='interrupt'.

agent/core/session.py: added self.is_in_tool_call: bool = False in Session.__init__. OpType.INTERRUPT and OpType.SHUTDOWN already existed (lines 59, 62).

agent/core/agent_loop.py: wrapped both asyncio.gather tool execution blocks (non-approval and approved-after-batch-approval) with try/finally setting is_in_tool_call=True/False.

agent/tools/research_tool.py: wrapped sub-agent's tool_router.call_tool with same try/finally.

tests/test_session_manager_submissions.py (NEW, 18 tests): enqueue paths, holder/non-holder interrupt, FIFO drain, inline interrupt/shutdown handling, poison containment, store-disabled paths, _build_operation reconstruction.
Adjusted 3 existing test files to remove submission_queue=asyncio.Queue() kwargs.

Verification:
- ruff: clean on all modified files
- pytest: 55 tests pass in verification suite (12 + 12 + 18 + 6 + 5 + 2 inline = previous + new)
- Broader pytest tests/ --ignore=tests/integration: 248 pass; 2 pre-existing failures in test_doom_loop.py confirmed unrelated via git stash

Edge case flagged by executor: standalone agent/main.py CLI uses its own submission_loop/asyncio.Queue (untouched). When MongoSessionStore is disabled at boot but a session was created earlier (CLI/test), submit* returns False rather than queuing in-memory. Acceptable for hosted backend (Mongo is required there).

## Iteration 4: US-005 (Step 4 — holder-local fast-path overlay) — COMPLETE

backend/session_manager.py:
- Added time import.
- Added holder_id: str | None = None to AgentSession dataclass; set when lease claimed in create_session and ensure_session_loaded.
- Added _subscriber_counts: dict[str, int] and _no_subscriber_since: dict[str, float] to __init__.
- Added _attach_subscriber and _detach_subscriber methods (used by US-006's grace sweeper).

backend/routes/agent.py:
- Added PyMongoError import.
- _sse_response signature is now (session_id, agent_session, *, replay_events, after_seq). Replay-then-live with two transport branches:
  - Holder fast path: subscribe to agent_session.broadcaster (in-process)
  - Non-holder slow path: tail store.change_stream_events; fall back to 500ms load_events_after poll on PyMongoError/NotImplementedError
- Both branches call _attach_subscriber/_detach_subscriber. Keepalives every 15s preserved.
- chat_sse no longer subscribes pre-submit (Mongo durability makes it unnecessary). Enqueues, then loads replay events, calls new _sse_response.
- subscribe_events updated to new signature.

agent/core/session.py: durability invariant verified — send_event calls append_event BEFORE event_queue.put (lines 157-165). No changes needed there.

tests/test_sse_holder_overlay.py NEW (7 tests): holder fast path uses broadcaster; non-holder uses change stream; non-holder falls back to poll on PyMongoError; subscriber counter attach/detach for both paths; replay phase skips seqs ≤ after_seq; terminal event in replay ends stream.

Verification:
- ruff: clean on session_manager.py + tests; routes/agent.py has 2 pre-existing errors (os import unused line 10, agent_session unused line 572) verified via git stash as predating this PR
- pytest: 62 passing in cumulative suite (12 + 12 + 18 + 7 + 5 + 5 + 3)

## Iteration 5: US-006 (Step 5 — lifespan sweep + grace sweeper + /background + migrating) — COMPLETE

backend/session_manager.py:
- Added _grace_sweep_task: asyncio.Task | None = None to __init__.
- start() creates _grace_sweep_task (_grace_period_sweep_loop) alongside heartbeat.
- close() cancels and awaits _grace_sweep_task.
- Added release_session_to_background(session_id, reason): emits migrating event via session.send_event (durably persisted via append_event), requeue_claimed_for, release_lease, drops from self.sessions, cancels task without await.
- Added _grace_period_sweep_loop: 30s tick, scans held sessions, releases when subscribers absent ≥ GRACE_PERIOD_SECONDS (default 180s) AND session has work (is_processing OR is_in_tool_call OR pending submissions). Idle-no-work sessions left alone (US-007 territory).

backend/main.py: lifespan shutdown sweep added between final-flush and session_manager.close(). Uses session_manager._runtime_state(agent_session).

backend/routes/agent.py: POST /api/session/{session_id}/background route. Reuses _check_session_access. Returns 200 {"status":"released"} or 404.

tests/test_lifespan_grace_sweep.py NEW (8 tests): release_session_to_background event emission, drop, idempotency; grace sweep with subscribers present (no release), grace expired with work (release), grace expired without work (no release); /background route 200/404.
tests/test_session_manager_lease.py + tests/test_session_manager_submissions.py: added manager._grace_sweep_task = None to _bare_manager helper.

Verification: ruff clean on session_manager.py + new tests; 8 pre-existing ruff errors in main.py and routes/agent.py verified unrelated via git stash. pytest: 70 passing in cumulative suite.

Subtlety: grace-sweep predicate requires BOTH (no subs ≥ grace period) AND (has work). This avoids evicting brand-new tabs the user opened, walked away from, and never typed in.

## Iteration 6: US-007 (Step 6 — Worker entrypoint + MODE flag + idle eviction) — COMPLETE

backend/start.sh: rewritten to 21 lines. MODE=worker branch (exec python -m worker) FIRST, preserves Main port-conflict graceful exit-0 hack.

backend/worker.py NEW (7-line shim): asyncio.run(worker_loop()). Uses `from main import worker_loop` to match WORKDIR=/app/backend import contract.

backend/main.py: added asyncio import. Added _worker_claim_tick() (polls pending_submissions for status=pending, skips held session_ids, delegates to claim_dormant_session). Added worker_loop() (calls session_manager.start, runs tick at 1s cadence with 2s back-off on errors, calls close on shutdown).

backend/session_manager.py:
- _idle_eviction_task field; _idle_eviction_loop method (60s tick, drops held sessions past IDLE_EVICTION_SECONDS default 1800s AND not processing AND not is_in_tool_call AND no pending submissions in Mongo); start/close manage the task.
- _rebuild_agent_session_from_store extracted from ensure_session_loaded; shared with new claim_dormant_session.
- claim_dormant_session(session_id) — bypasses user-ownership check (process-level trust); lease CAS still enforces one-holder invariant.
- last_submission_at switched from asyncio.get_event_loop().time() (monotonic) to time.time() (wall clock) for consistency with _no_subscriber_since and idle predicate.

tests/test_worker_idle_eviction.py NEW (13 tests): idle eviction predicate matrix, claim_dormant_session bypass + lease-taken paths, worker claim tick skips held sessions.
3 existing test files: added manager._idle_eviction_task = None to _bare_manager() fixtures.

Verification:
- bash -n backend/start.sh: syntax OK
- ruff: clean on all changed files; 6 pre-existing E402 in main.py from intentional load_dotenv ordering (verified via git stash)
- pytest: 83 passed, 0 failed in cumulative suite

Subtleties: worker claim tick polls db.pending_submissions directly rather than walking sessions (works without prior knowledge of session set). last_submission_at unified to time.time() wall-clock.

## Iteration 7: starting US-008 (Step 7 — observability + deployment migration)

Smallest story. Add log lines for lease ops + replay scan + pending lag. Add deployment runbook (deploy ordering: Workers first, Main last; backfill via init).






